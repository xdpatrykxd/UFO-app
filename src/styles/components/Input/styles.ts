import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 50,
    borderColor: '#ccc',
    borderWidth: 1,
    marginBottom: 15,
    paddingLeft: 10,
    color: 'white',
    backgroundColor: '#333',
  },
});

export default styles;